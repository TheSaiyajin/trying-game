{
  "name": "trying-game",
  "version": "1.0.0",
  "private": true,
  "description": "Territory Conquest multiplayer test version with Express + PostgreSQL backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "db:init": "node backend/db.js --init",
    "test": "node --test tests/*.js",
    "build": "node --check backend/server.js && node --check script.js && node --check world-topology.js && node --check backend/topology-sql.js && node --check backend/admin-bootstrap.js && node --check backend/season.js && node --check backend/season-time.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.18.0"
  }
}
